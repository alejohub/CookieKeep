import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';
test('shared readable typography scale replaces tiny secondary sizes',async()=>{const css=await readFile('src/shared.css','utf8');for(const [name,size] of [['title',28],['section',18],['body',14],['secondary',13]])assert.ok(css.includes('--font-size-'+name+':'+size+'px'));assert.ok(!/font-size:(?:11|12)px|font:12px/.test(css));assert.ok(css.includes('.preview-details-link'));assert.ok(css.includes('text-decoration:underline'));});
